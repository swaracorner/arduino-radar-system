# arduino-radar-system
Using an Arduino a simple radar system that detects nearby objects and displays their position in real time .Ultrasonic sensor scans surroundings Servo motor rotates the sensor Data visualized on a computer screen As the sensor sweeps across an area, it sends signals that bounce off objects.
